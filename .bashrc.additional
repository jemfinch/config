# some more ls aliases
alias ls='ls --color=auto'
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

source ~/.envrc

alias ack="ack-grep -a"
umask 0007

HISTSIZE=1000000
HISTFILESIZE=2000000000

shopt -s histappend
